apiVersion: batch/v1
kind: Job
metadata:
  name: kubestash-demo-data-verify
  namespace: demo
spec:
  backoffLimit: 1
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: verify
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              cat /root-disk/sample.txt /data-disk/sample.txt
              sha256sum /root-disk/sample.txt /data-disk/sample.txt
          volumeMounts:
            - { name: root, mountPath: /root-disk }
            - { name: data, mountPath: /data-disk }
      volumes:
        - name: root
          persistentVolumeClaim:
            claimName: kubestash-demo-vm-root
        - name: data
          persistentVolumeClaim:
            claimName: kubestash-demo-vm-data
